{
  "name": "dbviewer",
  "displayName": "MySQL Simple Viewer",
  "description": "MySQL database viewer for VS Code",
  "version": "0.0.3",
  "publisher": "cxwang",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:dbviewer.databasesView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "dbviewer",
          "title": "DB Viewer",
          "icon": "resources/database.svg"
        }
      ]
    },
    "views": {
      "dbviewer": [
        {
          "id": "dbviewer.databasesView",
          "name": "MySQL Databases",
          "type": "tree"
        }
      ]
    },
    "commands": [
      {
        "command": "dbviewer.openTable",
        "title": "Open Table"
      },
      {
        "command": "dbviewer.exportConnections",
        "title": "导出连接",
        "icon": "$(cloud-download)"
      },
      {
        "command": "dbviewer.importConnections",
        "title": "导入连接",
        "icon": "$(cloud-upload)"
      },
      {
        "command": "dbviewer.addConnection",
        "title": "New Connection",
        "icon": "$(add)"
      },
      {
        "command": "dbviewer.editConnection",
        "title": "Edit Connection",
        "icon": "$(edit)"
      },
      {
        "command": "dbviewer.deleteConnection",
        "title": "Delete Connection",
        "icon": "$(trash)"
      },
      {
        "command": "dbviewer.refreshConnection",
        "title": "刷新连接",
        "icon": "$(refresh)"
      },
      
      {
        "command": "dbviewer.openQuery",
        "title": "Query",
        "icon": "$(search)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "dbviewer.addConnection",
          "when": "view == dbviewer.databasesView",
          "group": "navigation"
        },
        {
          "command": "dbviewer.exportConnections",
          "when": "view == dbviewer.databasesView",
          "group": "navigation"
        },
        {
          "command": "dbviewer.importConnections",
          "when": "view == dbviewer.databasesView",
          "group": "navigation"
        }
        
      ],
      "view/item/context": [
        {
          "command": "dbviewer.editConnection",
          "when": "view == dbviewer.databasesView && viewItem == connection",
          "group": "inline"
        },
        {
          "command": "dbviewer.deleteConnection",
          "when": "view == dbviewer.databasesView && viewItem == connection",
          "group": "inline"
        },
        {
          "command": "dbviewer.openQuery",
          "when": "view == dbviewer.databasesView && viewItem == database",
          "group": "inline"
        },
        {
          "command": "dbviewer.openQuery",
          "when": "view == dbviewer.databasesView && viewItem == table",
          "group": "inline"
        }
      ,
        {
          "command": "dbviewer.refreshConnection",
          "when": "view == dbviewer.databasesView && viewItem == connection",
          "group": "inline"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run lint && npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run lint && npm run compile",
    "lint": "eslint src",
    "test": "npm run lint && npm run compile && vscode-test",
    "package": "vsce package --allow-missing-repository --allow-star-activation"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.13",
    "@types/vscode": "^1.80.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  },
  "dependencies": {
    "mysql2": "^3.18.2",
    "ssh2": "^1.13.0"
  }
}
